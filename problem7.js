// function squareDigits(num) {
//     let square = 0;
//     for (let i = 1; i < num; i++)

// }
// console.log(squareDigits(4114));



function squareDigits(num) {
    let numStr = String(num) //convert number into string
    for(let i = 0; i < numStr.length; i++){
        console.log(numStr[i] * numStr[i])
        let joinArr = numStr
    }

}

squareDigits(232)


